name: Security
on:
  push:
    branches: [main, development]
  pull_request:
  schedule:
    - cron: '17 6 * * 1' # Mondays, for the weekly report
jobs:
  security:
    uses: jubasjl76-eng/smart-pet-ci/.github/workflows/security-scan.yml@main
    with:
      node: true
      iac: true # Dockerfile
